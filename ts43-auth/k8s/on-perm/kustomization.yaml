apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ts43

resources:
  - ../base

# Pin/override your image/tag at deploy-time
images:
  - name: us-central1-docker.pkg.dev/sherlockapi/hudson/ts43-auth
    newTag: latest  # change to a git SHA/tag you build

patches:
  - path: patch-env.yaml
    target:
      kind: Deployment
      name: ts43-auth
